# Server-side key (NEVER prefixed with VITE_). Read by the Vercel function
# and by the Vite dev proxy. This key stays on the server and is NOT shipped
# to the browser, so users cannot see it in the built app.
FOOTBALL_DATA_API_KEY=your_api_key_here
