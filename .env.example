# No environment variables are required. All football-data.org data is baked
# into src/data/realFootballData.json at build time (see scripts/fetchData.mjs).
# To refresh the data, edit the key in scripts/fetchData.mjs and run it.
FOOTBALL_DATA_API_KEY=your_api_key_here
